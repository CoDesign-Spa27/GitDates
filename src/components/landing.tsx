import Image from "next/image";

const Landing = () => {
  return (
    <div>
 
      Landing
      <Image src={"/code.svg"} alt="code" width={100} height={100} />
    </div>
  );
};

export default Landing;
